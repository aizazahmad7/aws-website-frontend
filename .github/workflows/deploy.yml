name: Deploy Infra (Terraform)

on:
  workflow_dispatch: {}   # ⬅ only manual trigger; remove any "push"/"pull_request" blocks

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled for now (manual only)."
